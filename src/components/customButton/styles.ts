import { styled } from "styled-components";


export const Button = styled.button`
    background: var(--blue400);
    padding: 18px 15px;
    color: var(--white);
    border-radius: 8px;
    margin-top: 20px;
`;